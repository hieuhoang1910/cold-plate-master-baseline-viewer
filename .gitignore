# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env
.pytest_cache/

# Node / frontend (Phase 2+)
node_modules/
frontend/dist/
dist/
build/
*.tsbuildinfo
*.local
npm-debug.log*
yarn-error.log*

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Local user snapshots (spec §15 Q4 — server-side save is deferred)
snapshots/

# Server-local user projects (V2.2 — LAN-shared via the API, not git-tracked;
# the GB202 built-in preset lives in code and is always available)
projects/
